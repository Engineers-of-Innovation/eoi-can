[package]
name = "eoi-boot"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "eoi-boot"
path = "src/main.rs"

[features]
# Exactly one of these must be enabled. They match the app's [[bin]] names.
rudder-controller = []
height-sensor-controller = []
dashboard = []

[dependencies]
embassy-executor = { git = "https://github.com/embassy-rs/embassy", tag = "embassy-stm32-v0.6.0", features = ["platform-cortex-m", "executor-thread", "defmt"] }
embassy-stm32 = { git = "https://github.com/embassy-rs/embassy", tag = "embassy-stm32-v0.6.0", features = ["stm32l471rg", "defmt", "time-driver-tim4"] }
embassy-futures = { git = "https://github.com/embassy-rs/embassy.git", tag = "embassy-stm32-v0.6.0" }
embassy-time = { git = "https://github.com/embassy-rs/embassy.git", tag = "embassy-stm32-v0.6.0", features = ["defmt-timestamp-uptime", "tick-hz-32_768"] }
embassy-sync = { git = "https://github.com/embassy-rs/embassy", tag = "embassy-stm32-v0.6.0", features = ["defmt"] }
panic-probe = { version = "1.0.0", features = ["print-defmt"] }
cortex-m = { version = "0.7.7", features = ["critical-section-single-core"] }
cortex-m-rt = "0.7.0"
defmt = "0.3.10"
defmt-rtt = "0.4"
embedded-storage = "0.3"
eoi-boot-api = { path = "../boot-api", features = ["defmt"] }
static_cell = "2.1.1"
