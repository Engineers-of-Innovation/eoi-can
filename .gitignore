target/
Cargo.lock
screenshot.png
*.log
*.csv
# ...except the foiling key map, which is a generated *source* file: the display's
# layout tests `include_str!` it to check the datalogger's copy has not drifted, so
# a clone without it does not compile.
!FOILING_PARAMETERS.csv
flamegraph.svg
perf.data*
.env

# Python bytecode from support/ scripts
__pycache__/
backups
