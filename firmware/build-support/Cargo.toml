[package]
name = "eoi-build-support"
version = "0.1.0"
edition = "2024"

# Build-script logic shared by every application crate. A library rather than a
# copy in each `build.rs`, because `cargo:rustc-link-arg-bins` is package-scoped
# -- it cannot be emitted once from `eoi-firmware-common` and inherited -- and
# duplicating the flash layout is exactly the kind of drift that bricks a board.
[dependencies]
